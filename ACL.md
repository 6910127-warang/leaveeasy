# ACL — ตารางสิทธิ์ผู้ใช้ (LeaveEasy)

> อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 (ผู้ใช้ 3 บทบาท) เอกสารนี้แค่แจกแจงตารางเดิมให้อ่านง่ายขึ้น ไม่ได้เพิ่มสิทธิ์หรือกฎใหม่ใด ๆ

| บทบาท                       | ทำได้                                                                                                                                                                             | ทำไม่ได้                                                                                                                                     |
| --------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 🙋 **พนักงาน** (`employee`) | • ยื่นใบขอลาใหม่<br>• ดูใบลาของตัวเอง<br>• เขียนความเห็นเพิ่มในใบของตัวเอง<br>• ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา`                                                          | • เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ)<br>• ดูใบลาของคนอื่น<br>• แก้ไขประเภทการลา<br>• ลบใบลาที่ไม่ใช่ของตัวเอง หรือใบที่ผ่านการพิจารณาแล้ว |
| ✅ **หัวหน้า** (`manager`)   | • ดูใบลาทุกใบ<br>• เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ) — เปลี่ยนได้ครั้งเดียว เปลี่ยนกลับไม่ได้<br>• เขียนความเห็นในทุกใบ                                                       | • ลบใบลาของคนอื่น<br>• แก้ไขประเภทการลา                                                                                                      |
| 🧾 **ฝ่ายบุคคล** (`hr`)     | ทำได้ทุกอย่างที่หัวหน้าทำได้ เพิ่มเติมคือ:<br>• กำหนดผู้อนุมัติให้แต่ละใบ<br>• เพิ่ม/แก้/ลบ ประเภทการลา<br>• ดูแดชบอร์ดสรุปทั้งระบบ (หน้าโครงจาก prototype — ยังไม่ต่อข้อมูลจริง) | —                                                                                                                                            |

## หมายเหตุสถานะปัจจุบันของโครงการ

- **ยังไม่มีการบังคับสิทธิ์จริง** ตารางนี้เป็นสเปกที่ตั้งใจไว้ ไม่ใช่สิ่งที่ระบบบังคับอยู่ตอนนี้:
  - สัปดาห์ที่ 6 (ตอนนี้): ยังไม่มีล็อกอิน ทุกหน้าเปิดได้หมด งานเขียนต่าง ๆ ยัง hardcode ผู้ใช้ตัวอย่าง (`u001`/`u002`)
  - สัปดาห์ที่ 7: มีล็อกอินด้วย Firebase Authentication แล้ว แต่ยังเป็นแค่กฎขั้นต่ำ "ต้องล็อกอินก่อน" — ยังไม่แยกสิทธิ์ตามบทบาท
  - สัปดาห์ที่ 8: ตารางนี้จะเริ่มมีผลจริงผ่าน Firestore Security Rules ที่ตรวจค่าในช่อง `role`
- ค่าที่เก็บจริงในฟิลด์ `role` ของ Firestore ต้องเป็นรหัสอังกฤษตามตาราง (`employee` / `manager` / `hr`) เท่านั้น ส่วนชื่อบทบาทภาษาไทยใช้แสดงผลบนหน้าจอ
- สถานะใบลา (`status`) เปลี่ยนได้ทางเดียว: `รอพิจารณา` → `อนุมัติ` หรือ `ไม่อนุมัติ` เท่านั้น ไม่มีบทบาทใดเปลี่ยนกลับได้
